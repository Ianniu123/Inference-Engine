apiVersion: v1
kind: Service
metadata:
  name: inference-engine
  labels:
    app: inference-engine
spec:
  selector:
    app: inference-engine
  ports:
    - name: http
      port: 80
      targetPort: 8000
  type: ClusterIP
